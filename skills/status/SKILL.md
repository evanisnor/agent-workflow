---
name: status
description: "Display a status table of all active Task Agents, their current activity, and PR state. Invoke with /status."
---

# Status Skill

Render the agent status table immediately, following the template and rules defined in `skills/orchestrating-agents/STATUS.md`.

This skill contains no independent logic. All rendering rules, column definitions, activity values, and row inclusion criteria are defined in STATUS.md.
